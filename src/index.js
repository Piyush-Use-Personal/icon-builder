import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';




const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <App />
  
);


// clean code
// 2 react feather
// 3 create class of registrer
// 4 make component which uses 

